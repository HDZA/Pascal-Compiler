Convert from base 10 to binary BCD, hex.

Take 1 and 2's complements

Subtract y binary from x where x > y.

design a combo circuit that implements the given instructions. involves making the truth table, kmap, boolean expression, logic diagram.

Given a sequential sercut write the flip-flip equations create the state table, complete state diagram.

Hamming code question.

Memory is how many bytes.

Analyze a familar circut from chap six and tell what happens after each clock cycle'

Pal problem use k map, fill out pal program table, draw the x's on the diagram. 